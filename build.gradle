buildscript {
    ext.kotlin_version = "1.2.41"
    ext.coroutinesVersion = "0.23.1"
}

subprojects {

    repositories {
        mavenCentral()
        google()
        jcenter()
    }

    buildscript {

        repositories {
            mavenCentral()
        }

        dependencies {
            classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        }
    }
}
